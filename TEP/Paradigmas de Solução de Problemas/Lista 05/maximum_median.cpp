#include <bits/stdc++.h>
using namespace std;

#define all(xs) xs.begin(), xs.end()

signed main()
{
    int n, k;
    cin >> n >> k;

    vector<int> xs(n);
    for(auto& p : xs) cin >> p;

    sort(all(xs));

    

    return 0;
}