#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m, s;
    cin >> n >> m;
    int mapa[3][m];
    int conex[n];

    for(int i = 0; i < m; i++)
    {
        cin >> mapa[0][i] >> mapa[1][i] >> mapa[2][i];
    }

    cin >> s;

    for(int i = 0; i < 3; i++)
    {
        for(int j = 0; j < m; j++)
        {
            
        }
    }

    return 0;
}