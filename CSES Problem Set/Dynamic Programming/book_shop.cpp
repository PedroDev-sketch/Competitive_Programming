#include <bits/stdc++.h>
using namespace std;

signed main()
{
    int n, x;
    cin >> n >> x;

    vector<int> price(n), pages(n);
    for(auto& p : price) cin >> p;
    for(auto& p : pages) cin >> p;

    
}