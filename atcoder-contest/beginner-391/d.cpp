#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using vll = vector<ll>;
using pll = pair<ll,ll>;
using vpll = vector<pll>;

signed main()
{
    cin.tie(0) -> ios::sync_with_stdio(0);

    int n, w;
    cin >> n >> w;

    int x, y;
    for(int i = 0; i < n; i++)
    {
        cin >> x >> y;

        
    }

    return 0;
}